<!DOCTYPE html>
<html>
  <head>
    <title><%= content_for(:title) || "Tmp Rails App" %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <meta name="mobile-web-app-capable" content="yes">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= yield :head %>

    <%# Enable PWA manifest for installable apps (make sure to enable in config/routes.rb too!) %>
    <%#= tag.link rel: "manifest", href: pwa_manifest_path(format: :json) %>

    <link rel="icon" href="/icon.png" type="image/png">
    <link rel="icon" href="/icon.svg" type="image/svg+xml">
    <link rel="apple-touch-icon" href="/icon.png">

    <%# Includes all stylesheet files in app/assets/stylesheets %>
    <%= stylesheet_link_tag :app, "data-turbo-track": "reload" %>
    <%= javascript_include_tag "application", "data-turbo-track": "reload", type: "module" %>
  </head>

  <body class="bg-slate-50 text-slate-900">
    <header class="sticky top-0 z-30 border-b border-white/10 bg-white/80 backdrop-blur-xl">
      <nav class="mx-auto flex max-w-6xl items-center justify-between gap-6 px-4 py-4">
        <div class="flex items-center gap-6">
          <div class="flex items-center gap-2 text-xl font-semibold text-slate-900">
            SilverX Console
            <%= environment_badge %>
          </div>
          <% if user_signed_in? %>
            <div class="hidden items-center gap-2 md:flex">
              <%= nav_link_to "Dashboard", admin_dashboard_path %>
              <%= nav_link_to "Appels", admin_calls_path %>
            </div>
          <% end %>
        </div>

        <% if user_signed_in? %>
          <div class="flex items-center gap-4">
            <div class="text-right">
              <p class="text-sm font-semibold text-slate-900"><%= current_user.email %></p>
              <p class="text-xs text-slate-500 capitalize">Rôle · <%= current_user.role %></p>
            </div>
            <div class="flex items-center gap-3">
              <div class="flex h-10 w-10 items-center justify-center rounded-full bg-slate-900 text-sm font-semibold text-white">
                <%= user_initials(current_user) %>
              </div>
              <%= button_to "Déconnexion", destroy_user_session_path,
                    method: :delete,
                    class: "inline-flex items-center rounded-full bg-slate-900 px-4 py-2 text-sm font-semibold text-white hover:bg-slate-700" %>
            </div>
          </div>
        <% end %>
      </nav>

      <% if user_signed_in? %>
        <div class="border-t border-slate-100 bg-white px-4 py-3 md:hidden">
          <div class="flex flex-wrap gap-2">
            <%= nav_link_to "Dashboard", admin_dashboard_path, class: "w-full justify-center md:w-auto" %>
            <%= nav_link_to "Appels", admin_calls_path, class: "w-full justify-center md:w-auto" %>
          </div>
        </div>
      <% end %>

      <% if notice.present? %>
        <p class="flash flash--notice"><%= notice %></p>
      <% end %>
      <% if alert.present? %>
        <p class="flash flash--alert"><%= alert %></p>
      <% end %>
    </header>

    <main class="app-shell__content">
      <%= yield %>
    </main>
  </body>
</html>
