# Scripts de bench

- `latency.ts` : mesure texte→audio (TTS) et voix→voix (E2E).
- `barge_in.ts` : test d’interruption (cut-through).

Pré-requis :
- `.env` rempli (providers en mode sandbox).
- `docker-compose up` en cours d’exécution.

TODO : implémentations à venir (Semaine 2 - Bench).
